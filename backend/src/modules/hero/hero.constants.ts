import {
  ButtonTarget,
  ButtonVariant,
  HeroStatus,
  TextAlign,
} from "./hero.types.js";

export const HERO_STATUS = Object.values(HeroStatus);

export const HERO_TEXT_ALIGN = Object.values(TextAlign);

export const HERO_BUTTON_VARIANT = Object.values(ButtonVariant);

export const HERO_BUTTON_TARGET = Object.values(ButtonTarget);

export const MAX_TITLE_LENGTH = 120;

export const MAX_SUBTITLE_LENGTH = 200;

export const MAX_DESCRIPTION_LENGTH = 600;